<Window x:Class="Bookworm.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bookworm.Windows"
        mc:Ignorable="d"
        Title="Bookworm" Height="520" Width="520"
        FocusManager.FocusedElement="{Binding ElementName=TalkButton}"
        PreviewKeyDown="Window_PreviewKeyDown"
        PreviewKeyUp="Window_PreviewKeyUp">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="TalkButton" Grid.Row="0"
                Content="Talk"
                Height="90" FontSize="28"
                AutomationProperties.Name="Talk"
                AutomationProperties.HelpText="Press and hold, or press Space or Enter anywhere in this window, to speak to Bookworm."
                PreviewMouseLeftButtonDown="TalkButton_PreviewMouseLeftButtonDown"
                PreviewMouseLeftButtonUp="TalkButton_PreviewMouseLeftButtonUp"/>

        <TextBlock x:Name="StatusTextBlock" Grid.Row="1"
                   Margin="0,14,0,14" FontSize="16"
                   Text="{Binding StatusText}"
                   AutomationProperties.LiveSetting="Polite"/>

        <ListBox x:Name="TranscriptListBox" Grid.Row="2"
                 ItemsSource="{Binding Transcript}"
                 AutomationProperties.Name="Conversation transcript"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap" Margin="0,4,0,4">
                        <Run Text="{Binding Speaker, Mode=OneWay}" FontWeight="Bold"/>
                        <Run Text=": "/>
                        <Run Text="{Binding Text, Mode=OneWay}"/>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
